parameters:

    #
    # Resolvers
    #
    mmoreram.controllerextra.resolver.get_annotation_resolver.class: Mmoreram\ControllerExtraBundle\Resolver\GetAnnotationResolver

services:

    #
    # Resolvers
    #
    mmoreram.controllerextra.resolver.get_annotation_resolver:
        class: %mmoreram.controllerextra.resolver.get_annotation_resolver.class%
        tags:
            - { name: controller_extra.annotation }
